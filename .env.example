# 生产环境配置示例
# 部署时请在 Vercel 平台配置这些环境变量

# 数据库连接字符串（生产环境必须使用 PostgreSQL）
# 获取方式：在 Vercel 创建 Postgres 数据库后，会自动生成此连接字符串
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# JWT 密钥（生产环境必须修改为强随机字符串，建议使用 32 位以上随机字符）
# 可以通过以下方式生成：https://www.uuidgenerator.net/api/guid 或使用密码管理工具
JWT_SECRET="change-this-to-a-strong-random-string-in-production"

# JWT 过期时间
JWT_EXPIRES_IN="7d"

# 运行环境（生产环境固定为 production）
NODE_ENV="production"

# 前端 URL（部署完成后，替换为你的实际前端地址）
FRONTEND_URL="https://your-frontend-domain.vercel.app"
